/*
12. What is Arity in Javascript? Explain with a real time example.
*/

/*
 Arity is number of arguments in function.

 Example
 function traverse(arr){
    if(traverse.arguments.length === 1){
        arr.forEach(function(value){
            console.log(value);
        });
    }else{
        console.log(`${traverse.name} requires only one argument`);
    }
}
traverse([1,2,3,4]);
*/


